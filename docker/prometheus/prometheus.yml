global:
  scrape_interval: 15s

scrape_configs:
  # Backend .NET expose /metrics qua prometheus-net (thêm Phase 2)
  - job_name: "harness-api"
    static_configs:
      - targets: ["host.docker.internal:5080"]

  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
